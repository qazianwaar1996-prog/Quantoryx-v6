{
  "name": "quantoryx-frontend",
  "version": "6.0.0",
  "private": true,
  "description": "Quantoryx v6 — backend-integrated frontend for the Quantoryx quantitative trading platform",
  "scripts": {
    "build":      "node build/build.js",
    "build:prod": "node build/build.js --prod",
    "verify":     "node build/verify.js",
    "audit":      "node build/audit.js",
    "smoke":      "node build/smoke.js",
    "e2e":        "node build/e2e.js",
    "a11y":       "node build/a11y.js",
    "dev":        "node build/devserver.js 4173 8000",
    "serve:prod": "QX_TARGET=/Quantoryx-v6-Production.html node build/devserver.js 4174 8000",
    "test":       "npm run build && npm run build:prod && npm run verify && npm run audit && npm run smoke",
    "test:full":  "npm run test && npm run e2e && npm run a11y"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-react": "^7.23.3",
    "playwright": "^1.61.1"
  }
}
